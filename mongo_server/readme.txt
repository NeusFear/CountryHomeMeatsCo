Run install.bat

Complete
Run service as network service user.
Make a note of the Data Directory. If it's not `C:\Program Files\MongoDB\Server\5.0\data`, contact me
Don't install compass.

Install Mongodb Tools

To start the server, run start_server.bat

If you're running for the first time, you then need to run
`run_first_time.bat` to configure the server
